# Django Settings
SECRET_KEY='your-django-secret-key-here' # Replace with a strong, unique key
DEBUG=True # Set to False in production
ALLOWED_HOSTS='127.0.0.1,localhost' # Comma-separated list of allowed hosts

# Database Settings (PostgreSQL)
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=

# Fernet Encryption Key (Generate using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FERNET_KEY='your-fernet-encryption-key-here'

# Strava API Credentials
STRAVA_CLIENT_ID='your_strava_client_id'
STRAVA_CLIENT_SECRET='your_strava_client_secret'

CHROME_EXTENSION_ID='chrome-extension-id'
